class RocchioAlgorithm(object):
    """description of class"""
    #################################################################################
    ## @brief   Constructor
    #  @details This method initializes the class with:
    #           user_improvement ID's of the relevant documents
    #################################################################################    
    def __init__(self,user_improvement):


    def rocchio_algoritm(self,corpus, q,query_id):
        modified_q =  ( weight_q * a ) + (b * 1/abs(Dr) *sum(Dj)) - (c * 1/abs(Dnr) *sum(Dk))
        return modified_q